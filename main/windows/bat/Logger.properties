# Logger用プロパティ
# ログレベル(FATAL, ERROR, WARN, INFO, DEBUG, TRACEのいずれかを指定)
LOGGER_LEVEL=INFO
# 出力方法(FILE, CONSOLEのいずれかを指定、未指定の場合は出力しない)
LOGGER_HANDLER=FILE

# 以下はLOGGER_HANDLERにFILEを指定した場合に指定する
# 出力先ファイル
LOGGER_FILE_OUTPUT=%TEMP%\app.log
# ログファイルのローテート方式(SIZEを指定、未指定の場合はローテートしない)
LOGGER_FILE_ROTATE=SIZE
# ローテートのバックアップ世代数の上限
LOGGER_FILE_ROTATE_MAXNUMBER=100
# ローテート方式にSIZEを指定した場合、最大サイズ(KB)を指定する
LOGGER_FILE_ROTATE_MAXSIZE=1024
